; platformio_override.ini — local machine overrides (tracked, but keep minimal)
; Hardware environments live in platformio.ini under [env:ulanzi_tc001].
; Add personal overrides here (e.g. custom upload_port for OTA).
